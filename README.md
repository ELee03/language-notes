# Language Notes

A static personal language-learning site focused on Japanese, Korean bridges, grammar search, and sentence parsing.

Open `index.html` directly in a browser, or host the folder with GitHub Pages.
